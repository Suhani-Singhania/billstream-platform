spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/billstream
    username: postgres
    password: '062004'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true