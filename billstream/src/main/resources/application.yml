spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/billstream
    username: postgres
    password: '062004'

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # High-Performance Cache Routing Config for Phase 7 Rate Limiter
  data:
    redis:
      host: localhost
      port: 6379